# 学习架构 · ROADMAP

> 前端 → Java 全栈 · 8 周上手计划的详细拆解
> 时间预算：工作日 1.5h/天 + 周末 3h（约 10.5h/周）
> 总目标：2 个月内能接住公司的 Java 后端小任务，不在 code review 翻车
> 进度记录在 `PROGRESS.md`，本文件是「地图」，一般不改动。

---

## 阶段一（第 1-2 周）｜祛魅：把后端从黑盒变成透明的管子

### 第 1 周：Java 环境 + 语法速览（够用版）
- **目标**：电脑能跑 Java，看懂基本语法（用 TypeScript 类比，不死记）
- **知识点**
  - 装 JDK、IntelliJ IDEA 社区版
  - 建第一个 Java 项目，跑通 `main` 方法
  - 语法速览：变量/类型、类与对象、方法（对照 TS 的 class、function、type）
  - Maven 是什么（类比前端的 `package.json` + npm）
- **练习**：写几个小 Java 程序（打印、简单函数、一个类）
- **周末产出** ✅：能独立跑起来一个 Java `main` 程序，输出一段结果
- **掌握标准**：能说出 Java 的 `class`/`方法`/`类型` 对应前端的什么

### 第 2 周：第一个 Spring Boot 接口
- **目标**：跑通人生第一个后端接口，看懂请求链路
- **知识点**
  - Spring Boot 是什么（类比：像后端界的脚手架 + 框架）
  - 用 Spring Initializr 生成项目
  - `@RestController` + 第一个 `GET` 接口，返回 JSON
  - 请求链路：URL → Controller → 返回 JSON（先不接数据库）
  - 用 Postman/Apifox 发请求测接口
- **练习**：写 2-3 个返回不同 JSON 的 GET 接口
- **周末产出** ✅：Postman 里能跑通一个返回 JSON 的 `/hello` 接口
- **掌握标准**：能画出「我在 Postman 点发送 → 后端哪几步 → 返回」的流程

---

## 阶段二（第 3-6 周）｜CRUD 打天下：公司 80% 后端活的原型
> 主线项目：一个「任务清单（Todo）」后端

### 第 3 周：数据库基础 + 连上程序
- **目标**：让程序能读写数据库
- **知识点**
  - SQL 基础：建表、`INSERT/SELECT/UPDATE/DELETE`
  - 装本地数据库（PostgreSQL 或 MySQL）
  - 实体 Entity：Java 对象 ↔ 数据库表的映射
  - 用 Spring Data JPA 连数据库（初学者最省事的方案）
- **周末产出** ✅：程序能从数据库读出并返回一条 Todo 数据
- **掌握标准**：能说清「一张表」和「一个 Java 类」是怎么对应的

### 第 4 周：查（Read）+ 增（Create）
- **知识点**：GET 列表/详情接口；POST 新增 + 请求体（`@RequestBody`）
- **周末产出** ✅：能新增一条 Todo，也能查出全部 / 单条
- **掌握标准**：能解释 POST 的请求体是怎么变成数据库里一行的

### 第 5 周：改（Update）+ 删（Delete）
- **知识点**：PUT/PATCH 修改；DELETE 删除；路径参数 `@PathVariable`；HTTP 状态码怎么选
- **周末产出** ✅：一套完整的 Todo 增删改查接口
- **掌握标准**：能说出 200/201/404/400 分别什么时候用

### 第 6 周：健壮性（新手和"能上手"的分水岭）
- **知识点**：参数校验、空值处理、统一异常处理、统一返回格式
- **周末产出** ✅：加了校验和错误处理的 Todo 后端（乱传参数不会崩）
- **掌握标准**：能演示「传了非法参数，后端优雅地返回错误而不是 500 崩溃」

---

## 阶段三（第 7-8 周）｜向公司代码靠拢：为接真实任务做准备

### 第 7 周：分层 + 读别人的代码
- **知识点**：Controller / Service / Repository 三层分工；把项目重构成公司常见结构；练习读一个示例/开源项目
- **周末产出** ✅：把 Todo 项目重构成规范三层结构
- **掌握标准**：拿到一个陌生接口，能顺着三层找到「数据从哪来的」

### 第 8 周：改别人的代码 + 上线体验 + 启动结业考核
- **知识点**：给已有项目加一个新功能、定位并修一个 bug；用免费平台把项目部署上线一次
- **周末产出** ✅：项目成功上线（拿到一个能访问的链接）
- **收尾**：讨论「结业考核」真实项目选题（我会扫你的历史对话给候选方向）

---

## 贯穿全程的一条铁律
AI 可以帮你写，但**每段代码你必须能讲清楚它在干嘛**。每次用 AI 生成代码后，我会抽查你 1-2 个问题。这是"真懂"和"只会粘贴"的分界线。
